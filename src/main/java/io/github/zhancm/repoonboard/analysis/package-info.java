/**
 * Analysis-pipeline orchestration, result assembly, summaries, and reading
 * guidance.
 *
 * <p>This package coordinates analyzers through their explicit inputs and
 * outputs instead of embedding framework detection rules.</p>
 */
package io.github.zhancm.repoonboard.analysis;
