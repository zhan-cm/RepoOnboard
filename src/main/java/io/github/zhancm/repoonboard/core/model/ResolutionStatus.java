package io.github.zhancm.repoonboard.core.model;

/** Whether a public model relationship was resolved deterministically. */
public enum ResolutionStatus {
    CONFIRMED,
    AMBIGUOUS,
    UNRESOLVED
}
