package io.github.zhancm.repoonboard.core.model;

/** Framework-neutral component roles supported by the public report. */
public enum ComponentKind {
    CONTROLLER,
    REST_CONTROLLER,
    SERVICE,
    REPOSITORY,
    COMPONENT,
    CONFIGURATION
}
