package io.github.zhancm.repoonboard.analyzer.spring;

/** Spring component roles supported in V0.1. */
public enum SpringComponentKind {
    CONTROLLER,
    REST_CONTROLLER,
    SERVICE,
    REPOSITORY,
    COMPONENT
}
