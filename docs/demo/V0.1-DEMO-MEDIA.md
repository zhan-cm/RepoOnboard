# RepoOnboard V0.1 Demo Media

## Published artifact

[![RepoOnboard V0.1 condensed demo](./media/repoonboard-v0.1-demo.gif)](./media/repoonboard-v0.1-demo.gif)

| Field | Value |
| --- | --- |
| Artifact | [`media/repoonboard-v0.1-demo.gif`](./media/repoonboard-v0.1-demo.gif) |
| Format | Animated GIF, looping |
| Dimensions | 960 × 600 |
| Frames | 5 |
| Duration | 14.4 seconds |
| File size | 394,927 bytes |
| SHA-256 | `00E606F4E517CC26A56EC5936F0FED5C529C782EA5D27EA10E05F1652B1761F3` |
| Captured | 2026-09-13 |

The GIF is deliberately short enough for the README while preserving readable
product labels, selected evidence, and the `PARTIAL` state. It is a condensed
walkthrough, not a claim that checkout and analysis finish in 14.4 seconds.

## Fixed capture inputs

| Input | Fixed value |
| --- | --- |
| RepoOnboard source commit | `2e27197da23d6b535d980c2dbd0f89020d57af68` |
| Packaged candidate | `target/repoonboard.jar` from `mvnw clean verify` |
| Packaged JAR SHA-256 | `3290C18FDED9CE4B3ADBEFBE9D6097844F0DE6FA3724C853F03336EE38A659C3` |
| Demo repository | [`jhipster/jhipster-sample-app`](https://github.com/jhipster/jhipster-sample-app) |
| Demo commit | [`e06e87abe0be8a3a194381ce651164a734811b3f`](https://github.com/jhipster/jhipster-sample-app/commit/e06e87abe0be8a3a194381ce651164a734811b3f) |
| Maven model cache | Intentionally empty |
| Analysis status | `PARTIAL` |

This is the current packaged V0.1 candidate, not a published Release. The demo
input is the exact licensed revision selected by T-1004; it was analyzed without
building, running, or modifying the target application.

## Scene contract

| Scene | Duration | Visible result |
| --- | ---: | --- |
| Scan | 2.4 s | Actual command and fixed report counts; `PARTIAL` is explicit |
| Overview | 2.8 s | Repository identity plus the coverage warning |
| Architecture | 3.2 s | `UserService` one-hop view with two confirmed repository edges and two unresolved relationships |
| API | 2.8 s | `POST /api/bank-accounts`, `createBankAccount`, source location, and mapping context |
| Start Here | 3.2 s | First recommended files, visible reasons, source Evidence, and coverage warning |

The opening scan card is a compact rendering of the real CLI output from the
same run. It is not synthetic analysis and does not represent elapsed scan time.
The remaining scenes are direct 1280 × 720 captures of the local packaged UI,
reduced to fit the README artifact.

## Verification record

- The demo checkout HEAD matched the fixed 40-character commit.
- `/api/report` and `/api/start-here` both returned HTTP 200.
- The captured report contained 1 module, 81 source files, 30 components, and
  24 endpoints; Start Here contained 24 recommendations.
- `PARTIAL` remained visible in Overview, Architecture, API, and Start Here.
- The Architecture scene showed only confirmed graph edges and retained the
  unresolved count.
- The demo checkout had an empty `git status --short` after capture.
- The GIF was reopened successfully as 5 frames totaling 14,400 ms.

The complete checkout and result contract remains in
[V0.1 Demo Repository](./V0.1-DEMO.md).
